---
_unbound_munin_path: /usr/local/etc/munin
_unbound:
  pip: py27-pip
  user: unbound
  conf_dir: /usr/local/etc/unbound
  dependencies:
    - libevent
    - libsodium
    - ldns
    # build dependencies
    - autoconf
    - automake
    - libtool
    - pkgconf
