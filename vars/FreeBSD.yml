---
_unbound:
  pip: py27-pip
  # python_dev:
  # libffi_dev:
  # openssl_dev:
  user: unbound
  conf_dir: /usr/local/etc/unbound
  dependencies:
    - libevent
    - libsodium
    - ldns
    # build dependencies
    - autoconf
    - automake
    - libtool
    - pkgconf
