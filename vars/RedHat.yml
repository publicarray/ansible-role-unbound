---
_unbound:
  pip: python2-pip
  user: unbound
  conf_dir: /etc/unbound
  pidfile: /var/run/unbound.pid
  dependencies:
    - ldns-devel
    - libevent-devel
    - libsodium-devel
    - expat-devel
    - openssl-devel
    - make
    - gcc
    - tar
