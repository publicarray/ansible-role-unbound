---
_unbound_munin_path: /etc/munin
_unbound:
  pip: python-pip
  cron: cron
  user: unbound
  conf_dir: /etc/unbound
  reloadcmd: service unbound restart
  pidfile: /var/run/unbound.pid
  dependencies:
    - ca-certificates
    - libldns-dev
    - libevent-dev
    - libexpat1-dev
    - libsodium-dev
    - libssl-dev
    - libc-dev
    # - libc6-dev
    - musl-dev
    - make
    - gcc
    - tar
