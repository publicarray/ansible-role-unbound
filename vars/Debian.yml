---
_unbound:
  pip: python-pip
  user: unbound
  conf_dir: /etc/unbound
  pidfile: /var/run/unbound.pid
  dependencies:
    - ca-certificates
    - libldns-dev
    - libevent-dev
    - libexpat1-dev
    - libsodium-dev
    - libssl-dev
    - libc-dev
    # - libc6-dev
    - musl-dev
    - make
    - gcc
    - tar
